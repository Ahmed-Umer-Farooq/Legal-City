# Production Environment Variables for Frontend

# Backend API URL (without /api suffix)
REACT_APP_BACKEND_URL=https://your-backend-domain.vercel.app

# OR use API URL (with /api suffix - will be stripped automatically)
REACT_APP_API_URL=https://your-backend-domain.vercel.app/api

# Stripe Publishable Key
REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_key

# Note: If neither REACT_APP_BACKEND_URL nor REACT_APP_API_URL is set,
# the app will use window.location.origin as the backend URL in production
